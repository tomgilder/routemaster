class SystemNav {
  static void back() {
    throw UnsupportedError('Only supported on web');
  }

  static void replaceLocation(String location) {
    throw UnsupportedError('Only supported on web');
  }
}
